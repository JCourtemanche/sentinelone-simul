steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinelone-simulator/s1-simulator:latest'
      - '-f'
      - 'deployment/Dockerfile'
      - '.'

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinelone-simulator/s1-simulator:latest'

substitutions:
  _REGION: europe-west1
